#include "pch.h"
#include "../pch.h"
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <set>
using namespace std;
/*
int main()
{
	long long lone,x;
	
	int dight, a;
	lone = x = dight = 0;
	cin >> a;
	while (lone < a)
	{
		lone = lone * 10 + 1;
		dight++;
	}
	while (true)
	{
		if (lone % a == 0)
		{
			x = lone / a;
			cout << x << ' ' << dight << endl;
			return 0;
			break;
		}
		else {
			lone = lone * 10 + 1;
			dight++;
		}
	}
	
	return 0;
}*/